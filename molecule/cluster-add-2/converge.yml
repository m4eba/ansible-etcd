---
- name: get ip addresses
  hosts: all
  tasks:
    - name: import get ip
      import_tasks: ../resources/roles/get_ip/tasks/main.yaml
- name: prepare
  hosts: localhost
  tasks:
    - name: add instance4 to etcd group
      add_host:
        name: instance4
        groups:
          - etcd
      changed_when: false
    - name: add instance5 to etcd group
      add_host:
        name: instance5
        groups:
          - etcd
      changed_when: false

- import_playbook: ../../playbooks/cluster.yaml
