name: test
on:
  push
jobs:
  molecule:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        distro:
          - ubuntu2004
          - ubuntu1804
          - debian10
          - debian11
          - centos8
          - centos7
          - fedora34
        scenario: 
          - default
    steps:
      - name: Check out codebase
        uses: actions/checkout@v2

      - name: mkdir
        run: mkdir -p /home/runner/.ansible/collections/ansible_collections/m4eba/ 
        
      - name: link
        run: ln -s `pwd` /home/runner/.ansible/collections/ansible_collections/m4eba/etcd

      - name: Set up Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Install requirements
        run: pip3 install -r requirements.txt

      - name: Install test dependencies
        run: pip3 install ansible molecule[docker] docker

      - name: Run role tests
        run: >-
          molecule --version &&
          ansible --version &&
          docker --version &&
          molecule --debug test -s ${{ matrix.scenario }}
        env:
          PY_COLORS: '1'
          ANSIBLE_FORCE_COLOR: '1'
          MOLECULE_DISTRO: ${{ matrix.distro }}